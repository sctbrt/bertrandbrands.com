---
/**
 * B-POS — Dedicated product landing page for B-POS Restaurant POS.
 * Electric blue/violet/starlight accent palette. Hero + features + screenshots + pricing + FAQ.
 * Uses ServiceDetailLayout (HeaderUniversal + VisitorNotify included via layout).
 */
import ServiceDetailLayout from '../layouts/ServiceDetailLayout.astro';
import StickyMobileCTA from '../components/StickyMobileCTA.astro';
import { breadcrumbList, schemaGraph, PROVIDER, AREA_SERVED } from '../lib/schema';

const currentYear = new Date().getFullYear();
---

<ServiceDetailLayout
    title="B-POS — Offline-First Restaurant POS | Bertrand Brands"
    description="Full-suite restaurant POS for dine-in, bar, and takeout. Offline-first, runs on any device. From $59/mo."
    canonical="https://bertrandbrands.ca/bpos"
    ogTitle="B-POS — Restaurant POS That Works Offline"
    ogDescription="Full-suite restaurant POS for dine-in, bar, and takeout. Offline-first, runs on any device. From $59/mo."
    ogImage="https://bertrandbrands.ca/assets/bpos-og-hero.png"
    spotlightColor="rgba(96, 165, 250, 0.10)"
    accentRgb="245, 158, 11"
    serviceName="B-POS Restaurant POS"
    serviceTier="software"
    serviceSlug="bpos"
    servicePrice="59"
    schemaJson={schemaGraph(
        breadcrumbList([
            { name: "Bertrand Brands", url: "https://bertrandbrands.ca/" },
            { name: "B-POS", url: "https://bertrandbrands.ca/bpos" }
        ]),
        {
            "@type": "SoftwareApplication",
            "name": "B-POS Restaurant POS",
            "url": "https://bertrandbrands.ca/bpos",
            "applicationCategory": "BusinessApplication",
            "operatingSystem": "Web, macOS, Windows, iOS, Android",
            "description": "Offline-first, full-suite restaurant point of sale for dine-in, bar, and takeout. Runs on any device.",
            "provider": PROVIDER,
            "areaServed": AREA_SERVED,
            "offers": [
                {
                    "@type": "Offer",
                    "name": "B-POS Standard",
                    "price": "59",
                    "priceCurrency": "CAD",
                    "priceSpecification": {
                        "@type": "UnitPriceSpecification",
                        "price": "59",
                        "priceCurrency": "CAD",
                        "billingDuration": "P1M"
                    },
                    "availability": "https://schema.org/InStock",
                    "description": "Full POS with orders, payments, floor plan, up to 3 terminals."
                },
                {
                    "@type": "Offer",
                    "name": "B-POS Pro",
                    "price": "129",
                    "priceCurrency": "CAD",
                    "priceSpecification": {
                        "@type": "UnitPriceSpecification",
                        "price": "129",
                        "priceCurrency": "CAD",
                        "billingDuration": "P1M"
                    },
                    "availability": "https://schema.org/InStock",
                    "description": "Everything in Standard plus unlimited terminals, KDS, inventory, advanced reporting."
                }
            ]
        }
    )}
>
    <Fragment slot="head">
        <style is:inline>
            /* ── BPOS Color Tokens ─────────────────────────── */
            :root {
                --bpos-blue: #60A5FA;
                --bpos-blue-bright: #93C5FD;
                --bpos-blue-dim: rgba(96, 165, 250, 0.12);
                --bpos-amber: #E8D5B0;
                --bpos-amber-dim: rgba(232, 213, 176, 0.08);
                --bpos-amber-glow: rgba(232, 213, 176, 0.06);
                --bpos-blue-glow: rgba(96, 165, 250, 0.08);
                --bpos-violet: #A78BFA;
                --bpos-violet-dim: rgba(167, 139, 250, 0.08);
            }

            /* ── Override parent Bertrand Brands warm tones ── */
            .service-page {
                background: #050810 !important;
                background-image:
                    radial-gradient(ellipse 120% 50% at 20% 0%, rgba(167, 139, 250, 0.04) 0%, transparent 50%),
                    radial-gradient(ellipse 100% 60% at 80% 100%, rgba(96, 165, 250, 0.03) 0%, transparent 50%) !important;
            }
            .service-page__spotlight--1,
            .service-page__spotlight--2 {
                display: none !important;
            }
            .sticky-cta__button {
                background: linear-gradient(135deg, #60A5FA, #3B82F6) !important;
                color: #050a14 !important;
                font-weight: 700 !important;
                box-shadow: 0 0 16px rgba(96, 165, 250, 0.3);
            }
            .sticky-cta {
                border-top: 1px solid rgba(96, 165, 250, 0.15) !important;
                background: rgba(5, 8, 16, 0.95) !important;
            }
            .service-back {
                color: var(--bpos-blue) !important;
                border-color: rgba(96, 165, 250, 0.15) !important;
                background: rgba(96, 165, 250, 0.04) !important;
            }
            .service-back:hover {
                border-color: rgba(96, 165, 250, 0.3) !important;
                background: rgba(96, 165, 250, 0.08) !important;
            }

            /* ── Sweep gradient dividers ──────────────────── */
            .bpos-sweep {
                height: 1px;
                max-width: 600px;
                margin: 0 auto;
                background: linear-gradient(90deg, transparent 0%, var(--bpos-blue) 20%, var(--bpos-violet) 50%, var(--bpos-amber) 80%, transparent 100%);
                opacity: 0.3;
                animation: bposSweepGlow 4s ease-in-out infinite;
                box-shadow: 0 0 12px rgba(167, 139, 250, 0.06), 0 0 24px rgba(96, 165, 250, 0.04);
            }

            /* ── Hero ──────────────────────────────────────── */
            .bpos-hero {
                padding: var(--space-lg) 0 var(--space-lg);
                text-align: center;
                position: relative;
                z-index: 1;
            }
            .bpos-hero::before {
                content: '';
                position: absolute;
                top: -10%;
                left: 50%;
                transform: translateX(-50%);
                width: 900px;
                height: 700px;
                background:
                    radial-gradient(ellipse 60% 50% at 40% 40%, rgba(167, 139, 250, 0.05) 0%, transparent 60%),
                    radial-gradient(ellipse 70% 60% at 60% 35%, rgba(96, 165, 250, 0.06) 0%, transparent 55%),
                    radial-gradient(ellipse 40% 30% at 50% 60%, rgba(232, 213, 176, 0.03) 0%, transparent 50%);
                pointer-events: none;
                z-index: -1;
            }
            .bpos-hero__logo {
                width: 400px;
                height: auto;
                margin: 0 auto var(--space-md);
                display: block;
                filter: drop-shadow(0 0 80px rgba(96, 165, 250, 0.3)) drop-shadow(0 0 120px rgba(167, 139, 250, 0.12)) drop-shadow(0 0 40px rgba(232, 213, 176, 0.08));
                mask-image: radial-gradient(ellipse 75% 70% at center 45%, black 30%, rgba(0,0,0,0.5) 50%, transparent 65%);
                -webkit-mask-image: radial-gradient(ellipse 75% 70% at center 45%, black 30%, rgba(0,0,0,0.5) 50%, transparent 65%);
                animation: bposLogoGlow 6s ease-in-out infinite;
            }
            @keyframes bposLogoGlow {
                0%, 100% { filter: drop-shadow(0 0 80px rgba(96, 165, 250, 0.3)) drop-shadow(0 0 120px rgba(167, 139, 250, 0.12)) drop-shadow(0 0 40px rgba(232, 213, 176, 0.08)); }
                50% { filter: drop-shadow(0 0 110px rgba(96, 165, 250, 0.45)) drop-shadow(0 0 160px rgba(167, 139, 250, 0.2)) drop-shadow(0 0 60px rgba(232, 213, 176, 0.12)); }
            }
            .bpos-hero__badge {
                display: inline-flex;
                align-items: center;
                gap: 6px;
                font-family: var(--font-body);
                font-size: var(--text-xs);
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: 0.1em;
                color: var(--bpos-blue-bright);
                border: 1px solid rgba(96, 165, 250, 0.2);
                border-radius: 20px;
                padding: 4px 14px;
                margin-bottom: var(--space-md);
                background: rgba(96, 165, 250, 0.05);
            }
            .bpos-hero__badge-dot {
                width: 6px;
                height: 6px;
                background: var(--bpos-blue-bright);
                border-radius: 50%;
                animation: bposPulse 2s ease-in-out infinite;
                box-shadow: 0 0 8px rgba(96, 165, 250, 0.6);
            }
            @keyframes bposPulse {
                0%, 100% { opacity: 1; transform: scale(1); }
                50% { opacity: 0.4; transform: scale(0.85); }
            }
            @keyframes bposSweepGlow {
                0%, 100% { opacity: 0.15; }
                50% { opacity: 0.35; }
            }
            .bpos-hero__title {
                font-family: var(--font-display);
                font-size: clamp(2rem, 5vw, 3.25rem);
                font-weight: 300;
                line-height: 1.15;
                color: rgba(255, 255, 255, 0.92);
                max-width: 720px;
                margin: 0 auto var(--space-sm);
            }
            .bpos-hero__title em {
                font-style: normal;
                font-weight: 500;
                background: linear-gradient(135deg, #60A5FA 0%, #818CF8 20%, #A78BFA 40%, #D4A76A 60%, #F5C842 80%, #FFE066 100%);
                background-size: 200% 200%;
                animation: heroGradientPool 28s ease-in-out infinite;
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                text-shadow: none;
                position: relative;
                filter: drop-shadow(0 0 20px rgba(96, 165, 250, 0.3)) drop-shadow(0 0 40px rgba(167, 139, 250, 0.15));
            }
            @keyframes heroGradientPool {
                0% { background-position: 0% 40%; }
                18% { background-position: 65% 20%; }
                38% { background-position: 100% 55%; }
                58% { background-position: 40% 80%; }
                78% { background-position: 10% 60%; }
                100% { background-position: 0% 40%; }
            }
            @media (prefers-reduced-motion: reduce) {
                .bpos-hero__title em {
                    animation: none;
                    background-size: 100% 100%;
                }
            }
            .bpos-hero__sub {
                font-family: var(--font-body);
                font-size: var(--text-lg);
                color: var(--text-muted);
                max-width: 560px;
                margin: 0 auto var(--space-sm);
                line-height: 1.6;
            }
            .bpos-hero__sub-custom {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-subtle);
                max-width: 440px;
                margin: 0 auto var(--space-lg);
                line-height: 1.5;
            }
            .bpos-hero__sub-custom span {
                color: var(--bpos-blue-bright);
                font-weight: 500;
            }
            .bpos-hero__actions {
                display: flex;
                justify-content: center;
                gap: var(--space-sm);
                flex-wrap: wrap;
            }
            .bpos-hero__cta {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                padding: 0.875rem 2rem;
                font-family: var(--font-body);
                font-size: var(--text-sm);
                font-weight: 600;
                text-decoration: none;
                border-radius: 10px;
                min-height: 48px;
                transition: all 0.2s ease;
            }
            .bpos-hero__cta--primary {
                background: linear-gradient(135deg, #60A5FA, #3B82F6);
                color: #050a14;
                font-weight: 700;
                box-shadow: 0 0 24px rgba(96, 165, 250, 0.35), 0 4px 12px rgba(0,0,0,0.3);
            }
            .bpos-hero__cta--primary:hover {
                box-shadow: 0 0 50px rgba(96, 165, 250, 0.55), 0 6px 16px rgba(0,0,0,0.4);
                transform: translateY(-2px);
            }
            .bpos-hero__cta--secondary {
                background: rgba(232, 213, 176, 0.06);
                border: 1px solid rgba(232, 213, 176, 0.2);
                color: var(--bpos-amber);
            }
            .bpos-hero__cta--secondary:hover {
                background: rgba(232, 213, 176, 0.12);
                border-color: rgba(232, 213, 176, 0.35);
                box-shadow: 0 0 20px rgba(232, 213, 176, 0.1);
                transform: translateY(-2px);
            }

            /* ── Features grid ─────────────────────────────── */
            .bpos-features {
                padding: var(--space-xl) 0;
                position: relative;
                z-index: 1;
            }
            .bpos-features__label {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: 0.12em;
                background: linear-gradient(90deg, var(--bpos-blue-bright), var(--bpos-violet), var(--bpos-amber));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                text-align: center;
                margin-bottom: var(--space-md);
            }
            .bpos-features__grid {
                display: grid;
                grid-template-columns: repeat(3, 1fr);
                gap: var(--space-md);
                max-width: 920px;
                margin: 0 auto;
            }
            .bpos-feature {
                background: rgba(96, 165, 250, 0.03);
                border: 1px solid rgba(96, 165, 250, 0.08);
                border-radius: var(--radius-lg);
                padding: var(--space-lg);
                transition: all 0.25s ease;
                position: relative;
            }
            .bpos-feature:hover {
                border-color: rgba(96, 165, 250, 0.25);
                box-shadow: 0 0 30px rgba(96, 165, 250, 0.08), 0 0 60px rgba(96, 165, 250, 0.03);
                transform: translateY(-2px);
                background: rgba(96, 165, 250, 0.05);
            }
            .bpos-feature__icon {
                font-size: 1.5rem;
                margin-bottom: var(--space-sm);
                line-height: 1;
                filter: drop-shadow(0 0 6px rgba(96, 165, 250, 0.4));
            }
            .bpos-feature__name {
                font-family: var(--font-body);
                font-size: var(--text-sm);
                font-weight: 600;
                color: var(--text);
                margin-bottom: 4px;
            }
            .bpos-feature__desc {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-muted);
                line-height: 1.5;
            }

            /* ── How it works ──────────────────────────────── */
            .bpos-how {
                padding: var(--space-xl) 0;
                position: relative;
                z-index: 1;
            }
            .bpos-how__label {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: 0.12em;
                background: linear-gradient(90deg, var(--bpos-blue-bright), var(--bpos-violet), var(--bpos-amber));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                text-align: center;
                margin-bottom: var(--space-lg);
            }
            .bpos-how__steps {
                display: flex;
                gap: var(--space-lg);
                max-width: 800px;
                margin: 0 auto;
                justify-content: center;
            }
            .bpos-step {
                flex: 1;
                text-align: center;
                max-width: 220px;
            }
            .bpos-step__num {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                width: 40px;
                height: 40px;
                border-radius: 50%;
                background: rgba(96, 165, 250, 0.08);
                border: 1px solid rgba(96, 165, 250, 0.2);
                color: var(--bpos-blue-bright);
                font-family: var(--font-body);
                font-size: var(--text-sm);
                font-weight: 700;
                margin-bottom: var(--space-sm);
                box-shadow: 0 0 16px rgba(96, 165, 250, 0.1);
            }
            .bpos-step__title {
                font-family: var(--font-body);
                font-size: var(--text-sm);
                font-weight: 600;
                color: var(--text);
                margin-bottom: 4px;
            }
            .bpos-step__desc {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-muted);
                line-height: 1.5;
            }

            /* ── Pricing ───────────────────────────────────── */
            .bpos-pricing {
                padding: var(--space-xl) 0;
                position: relative;
                z-index: 1;
            }
            .bpos-pricing__label {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: 0.12em;
                background: linear-gradient(90deg, var(--bpos-blue-bright), var(--bpos-violet), var(--bpos-amber));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                text-align: center;
                margin-bottom: var(--space-xs);
            }
            .bpos-pricing__headline {
                font-family: var(--font-display);
                font-size: clamp(1.25rem, 3vw, 1.75rem);
                font-weight: 300;
                color: var(--text);
                text-align: center;
                margin-bottom: var(--space-md);
            }

            /* Toggle */
            .bpos-toggle {
                display: flex;
                justify-content: center;
                gap: 2px;
                background: rgba(96, 165, 250, 0.04);
                border: 1px solid rgba(96, 165, 250, 0.12);
                border-radius: 10px;
                padding: 3px;
                max-width: 280px;
                margin: 0 auto var(--space-sm);
            }
            .bpos-toggle__btn {
                flex: 1;
                background: none;
                border: none;
                color: var(--text-subtle);
                font-family: var(--font-body);
                font-size: var(--text-xs);
                font-weight: 500;
                padding: 0.5rem 1rem;
                border-radius: 8px;
                cursor: pointer;
                transition: all 0.2s ease;
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 6px;
            }
            .bpos-toggle__btn.active {
                background: rgba(96, 165, 250, 0.12);
                color: var(--text);
                box-shadow: 0 0 8px rgba(96, 165, 250, 0.08);
            }
            .bpos-toggle__badge {
                font-size: 0.625rem;
                background: linear-gradient(135deg, var(--bpos-violet), var(--bpos-blue));
                color: #fff;
                padding: 1px 5px;
                border-radius: 3px;
                font-weight: 600;
                letter-spacing: 0.02em;
            }

            /* Early bird */
            .bpos-early-bird {
                text-align: center;
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-muted);
                margin-bottom: var(--space-md);
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 8px;
            }
            .bpos-early-bird__tag {
                font-size: 0.625rem;
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: 0.1em;
                color: var(--bpos-amber);
                border: 1px solid rgba(232, 213, 176, 0.4);
                border-radius: 3px;
                padding: 1px 6px;
                background: rgba(232, 213, 176, 0.06);
            }

            /* Plan grid */
            .bpos-plans {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: var(--space-sm);
                max-width: 720px;
                margin: 0 auto var(--space-md);
            }
            .bpos-plan {
                position: relative;
                background: linear-gradient(160deg, rgba(96, 165, 250, 0.04), rgba(232, 213, 176, 0.02));
                border: 1px solid rgba(96, 165, 250, 0.12);
                border-radius: 14px;
                padding: var(--space-lg);
                transition: all 0.25s ease;
            }
            .bpos-plan:hover {
                border-color: rgba(96, 165, 250, 0.2);
                box-shadow: 0 0 30px rgba(96, 165, 250, 0.05);
            }
            .bpos-plan--featured {
                border-color: rgba(232, 213, 176, 0.25);
                background: linear-gradient(160deg, rgba(232, 213, 176, 0.06), rgba(96, 165, 250, 0.03));
                box-shadow: 0 0 30px rgba(232, 213, 176, 0.04), 0 0 30px rgba(96, 165, 250, 0.03);
            }
            .bpos-plan--featured:hover {
                border-color: rgba(232, 213, 176, 0.35);
                box-shadow: 0 0 40px rgba(232, 213, 176, 0.08), 0 0 40px rgba(96, 165, 250, 0.05);
            }
            .bpos-plan__badge {
                position: absolute;
                top: -10px;
                left: 50%;
                transform: translateX(-50%);
                background: linear-gradient(135deg, var(--bpos-blue), var(--bpos-violet));
                color: #fff;
                font-family: var(--font-body);
                font-size: 0.625rem;
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: 0.08em;
                padding: 3px 10px;
                border-radius: 4px;
                white-space: nowrap;
                box-shadow: 0 0 12px rgba(96, 165, 250, 0.25), 0 0 24px rgba(167, 139, 250, 0.1);
            }
            .bpos-plan__name {
                font-family: var(--font-body);
                font-size: var(--text-lg);
                font-weight: 600;
                color: var(--text);
                margin-bottom: 2px;
            }
            .bpos-plan__tagline {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-muted);
                margin-bottom: var(--space-sm);
            }
            .bpos-plan__pricing {
                display: flex;
                align-items: baseline;
                gap: 6px;
                margin-bottom: 4px;
            }
            .bpos-plan__price-early {
                font-family: var(--font-display);
                font-size: clamp(1.5rem, 3vw, 2rem);
                font-weight: 600;
                color: var(--bpos-amber);
            }
            .bpos-plan__price-regular {
                font-family: var(--font-body);
                font-size: var(--text-sm);
                color: var(--text-subtle);
                text-decoration: line-through;
            }
            .bpos-plan__period {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-subtle);
            }
            .bpos-plan__savings {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--bpos-amber);
                min-height: 1.2em;
                margin-bottom: var(--space-sm);
            }
            .bpos-plan__features {
                list-style: none;
                padding: 0;
                margin: 0 0 var(--space-md);
            }
            .bpos-plan__features li {
                position: relative;
                padding-left: 1.25rem;
                margin-bottom: 6px;
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-muted);
                line-height: 1.5;
            }
            .bpos-plan__features li::before {
                content: '';
                position: absolute;
                left: 0;
                top: 0.4em;
                width: 5px;
                height: 5px;
                background: linear-gradient(135deg, var(--bpos-blue), var(--bpos-amber));
                border-radius: 50%;
            }
            .bpos-plan__cta {
                display: block;
                width: 100%;
                padding: 0.75rem 1rem;
                text-align: center;
                font-family: var(--font-body);
                font-size: var(--text-sm);
                font-weight: 600;
                text-decoration: none;
                border-radius: 10px;
                min-height: 44px;
                transition: all 0.2s ease;
                margin-bottom: 8px;
                cursor: pointer;
            }
            .bpos-plan__cta--primary {
                background: linear-gradient(135deg, #60A5FA, #3B82F6);
                color: #050a14;
                font-weight: 700;
                box-shadow: 0 0 16px rgba(96, 165, 250, 0.25);
            }
            .bpos-plan__cta--primary:hover {
                box-shadow: 0 0 30px rgba(96, 165, 250, 0.4);
                transform: translateY(-1px);
            }
            .bpos-plan__cta--subscribe {
                background: rgba(96, 165, 250, 0.08);
                border: 1px solid rgba(96, 165, 250, 0.25);
                color: var(--bpos-blue);
                font-weight: 600;
            }
            .bpos-plan__cta--subscribe:hover {
                background: rgba(96, 165, 250, 0.15);
                box-shadow: 0 0 16px rgba(96, 165, 250, 0.1);
                transform: translateY(-1px);
            }
            .bpos-plan__cta--secondary {
                background: rgba(255, 255, 255, 0.03);
                border: 1px solid rgba(255, 255, 255, 0.08);
                color: var(--text-muted);
                font-weight: 500;
                font-size: var(--text-xs);
            }
            .bpos-plan__cta--secondary:hover {
                background: rgba(255, 255, 255, 0.06);
                color: var(--text);
            }

            /* Trial modal */
            .bpos-trial-overlay {
                display: none;
                position: fixed;
                inset: 0;
                z-index: 9999;
                background: rgba(0,0,0,0.75);
                backdrop-filter: blur(4px);
                align-items: center;
                justify-content: center;
                padding: 24px;
            }
            .bpos-trial-overlay.is-open { display: flex; }
            .bpos-trial-modal {
                background: var(--bg-secondary, #0e1117);
                border: 1px solid rgba(96, 165, 250, 0.15);
                border-radius: 16px;
                padding: 32px;
                max-width: 420px;
                width: 100%;
                position: relative;
                box-shadow: 0 0 60px rgba(96, 165, 250, 0.06), 0 0 60px rgba(232, 213, 176, 0.04);
            }
            .bpos-trial-modal__title {
                font-family: var(--font-display, Inter, sans-serif);
                font-size: 18px;
                font-weight: 600;
                color: var(--text, #f5f5f5);
                margin-bottom: 4px;
            }
            .bpos-trial-modal__subtitle {
                font-size: 13px;
                color: var(--text-muted, #999);
                margin-bottom: 24px;
            }
            .bpos-trial-modal__close {
                position: absolute;
                top: 16px;
                right: 16px;
                background: none;
                border: none;
                color: var(--text-muted, #999);
                font-size: 20px;
                cursor: pointer;
                padding: 4px 8px;
                line-height: 1;
            }
            .bpos-trial-field {
                margin-bottom: 16px;
            }
            .bpos-trial-field label {
                display: block;
                font-size: 11px;
                font-weight: 600;
                letter-spacing: 0.08em;
                text-transform: uppercase;
                color: var(--text-subtle, #aaa);
                margin-bottom: 6px;
            }
            .bpos-trial-field input {
                width: 100%;
                padding: 10px 12px;
                font-size: 14px;
                font-family: var(--font-body, Inter, sans-serif);
                background: rgba(96, 165, 250, 0.04);
                border: 1px solid rgba(255,255,255,0.1);
                border-radius: 8px;
                color: var(--text, #f5f5f5);
                outline: none;
                transition: border-color 0.15s, box-shadow 0.15s;
            }
            .bpos-trial-field input:focus {
                border-color: rgba(96, 165, 250, 0.4);
                box-shadow: 0 0 12px rgba(96, 165, 250, 0.08);
            }
            .bpos-trial-submit {
                width: 100%;
                padding: 12px;
                font-size: 15px;
                font-weight: 700;
                font-family: var(--font-body, Inter, sans-serif);
                background: linear-gradient(135deg, #60A5FA, #3B82F6);
                color: #050a14;
                border: none;
                border-radius: 10px;
                cursor: pointer;
                margin-top: 8px;
                transition: all 0.2s;
                box-shadow: 0 0 16px rgba(96, 165, 250, 0.25);
            }
            .bpos-trial-submit:hover {
                box-shadow: 0 0 24px rgba(96, 165, 250, 0.4);
                transform: translateY(-1px);
            }
            .bpos-trial-submit:disabled {
                opacity: 0.6;
                cursor: not-allowed;
                box-shadow: none;
            }
            .bpos-trial-error {
                color: #ef4444;
                font-size: 12px;
                margin-top: 8px;
                display: none;
            }
            .bpos-trial-success {
                text-align: center;
                padding: 24px 0;
            }
            .bpos-trial-success__key {
                font-family: 'JetBrains Mono', monospace;
                font-size: 18px;
                font-weight: 600;
                color: var(--bpos-amber);
                letter-spacing: 0.04em;
                margin: 16px 0;
                padding: 12px;
                background: rgba(232, 213, 176, 0.06);
                border: 1px solid rgba(232, 213, 176, 0.2);
                border-radius: 8px;
            }
            .bpos-pricing__footnote {
                text-align: center;
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-subtle);
            }

            /* ── Custom builds banner ─────────────────────── */
            .bpos-custom {
                padding: var(--space-lg) 0;
                position: relative;
                z-index: 1;
            }
            .bpos-custom__card {
                max-width: 700px;
                margin: 0 auto;
                padding: var(--space-lg);
                border-radius: 14px;
                border: 1px solid rgba(96, 165, 250, 0.15);
                background: linear-gradient(135deg, rgba(96, 165, 250, 0.04), rgba(232, 213, 176, 0.02));
                text-align: center;
            }
            .bpos-custom__title {
                font-family: var(--font-body);
                font-size: var(--text-sm);
                font-weight: 600;
                color: var(--text);
                margin-bottom: 6px;
            }
            .bpos-custom__desc {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                color: var(--text-muted);
                line-height: 1.6;
                max-width: 500px;
                margin: 0 auto var(--space-sm);
            }
            .bpos-custom__link {
                display: inline-flex;
                align-items: center;
                gap: 4px;
                font-family: var(--font-body);
                font-size: var(--text-xs);
                font-weight: 600;
                color: var(--bpos-blue);
                text-decoration: none;
                transition: opacity 0.15s;
            }
            .bpos-custom__link:hover { opacity: 0.8; }

            /* ── FAQ ───────────────────────────────────────── */
            .bpos-faq {
                padding: var(--space-xl) 0;
                position: relative;
                z-index: 1;
            }
            .bpos-faq__label {
                font-family: var(--font-body);
                font-size: var(--text-xs);
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: 0.12em;
                background: linear-gradient(90deg, var(--bpos-blue-bright), var(--bpos-violet), var(--bpos-amber));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                text-align: center;
                margin-bottom: var(--space-lg);
            }
            .bpos-faq__list {
                max-width: 640px;
                margin: 0 auto;
            }
            .bpos-faq__item {
                border-bottom: 1px solid rgba(255, 255, 255, 0.06);
                padding: var(--space-md) 0;
            }
            .bpos-faq__item:first-child {
                border-top: 1px solid rgba(255, 255, 255, 0.06);
            }
            .bpos-faq__q {
                font-family: var(--font-body);
                font-size: var(--text-sm);
                font-weight: 600;
                color: var(--text);
                cursor: pointer;
                display: flex;
                justify-content: space-between;
                align-items: center;
                gap: var(--space-sm);
                width: 100%;
                background: none;
                border: none;
                text-align: left;
                padding: 0;
                min-height: 44px;
            }
            .bpos-faq__q:hover { color: var(--bpos-blue); }
            .bpos-faq__chevron {
                flex-shrink: 0;
                width: 16px;
                height: 16px;
                color: var(--text-subtle);
                transition: transform 0.2s ease;
            }
            .bpos-faq__item.is-open .bpos-faq__chevron {
                transform: rotate(180deg);
            }
            .bpos-faq__a {
                font-family: var(--font-body);
                font-size: var(--text-sm);
                color: var(--text-muted);
                line-height: 1.6;
                max-height: 0;
                overflow: hidden;
                transition: max-height 0.3s ease, padding 0.3s ease;
                padding-top: 0;
            }
            .bpos-faq__item.is-open .bpos-faq__a {
                max-height: 200px;
                padding-top: var(--space-sm);
            }

            /* ── Responsive ────────────────────────────────── */
            @media (max-width: 768px) {
                .bpos-hero { padding: var(--space-md) 0 var(--space-sm); }
                .bpos-hero__logo { width: 280px; }
                .bpos-hero__title { font-size: clamp(1.5rem, 6vw, 2rem); }
                .bpos-hero__sub { font-size: var(--text-sm); }
                .bpos-features__grid { grid-template-columns: 1fr 1fr; gap: var(--space-sm); }
                .bpos-feature { padding: var(--space-md); }
                .bpos-how__steps { flex-direction: column; align-items: center; gap: var(--space-md); }
                .bpos-step { max-width: 300px; }
                .bpos-plans { grid-template-columns: 1fr; max-width: 400px; }
                .bpos-plan { padding: var(--space-md); }
            }
            @media (max-width: 480px) {
                .bpos-features__grid { grid-template-columns: 1fr; }
                .bpos-hero__actions { flex-direction: column; align-items: center; }
                .bpos-hero__cta { width: 100%; max-width: 280px; }
            }

            @media (prefers-reduced-motion: reduce) {
                .bpos-hero__badge-dot { animation: none; opacity: 0.7; }
            }
        </style>
    </Fragment>

    <div class="service-page__spotlight service-page__spotlight--1" aria-hidden="true"></div>
    <div class="service-page__spotlight service-page__spotlight--2" aria-hidden="true"></div>

    <main class="service-page" id="main-content">
        <div class="container">
            <a href="/?skip#packages" class="service-back">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
                Back to Services
            </a>

            <!-- ── Hero ──────────────────────────────────── -->
            <section class="bpos-hero">
                <img src="/bpos-logo.png" alt="B-POS" class="bpos-hero__logo" width="400" height="400" />
                <div class="bpos-hero__badge">
                    <span class="bpos-hero__badge-dot"></span>
                    Restaurant POS Software
                </div>
                <h1 class="bpos-hero__title">
                    The POS that works<br><em>even when the WiFi doesn&rsquo;t</em>
                </h1>
                <p class="bpos-hero__sub">
                    Full-suite point of sale for dine-in, bar, and takeout.
                    Offline-first. Runs on any device. Built for real service.
                </p>
                <p class="bpos-hero__sub-custom">
                    Built for <span>restaurant brands</span> &mdash; with custom builds available for salons, retail, and other service businesses.
                </p>
                <div class="bpos-hero__actions">
                    <a href="https://pos.bertrandgroup.ca" target="_blank" rel="noopener noreferrer" class="bpos-hero__cta bpos-hero__cta--primary">Try B-POS Free</a>
                    <a href="https://pos.bertrandgroup.ca" target="_blank" rel="noopener noreferrer" class="bpos-hero__cta bpos-hero__cta--secondary">Try the Demo &rarr;</a>
                </div>
            </section>

            <div class="bpos-sweep" aria-hidden="true"></div>

            <!-- ── Features ──────────────────────────────── -->
            <section class="bpos-features">
                <p class="bpos-features__label">Everything You Need</p>
                <div class="bpos-features__grid">
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#9635;</div>
                        <p class="bpos-feature__name">Floor Plan & Tables</p>
                        <p class="bpos-feature__desc">Visual table management with real-time status. Seat guests, merge tables, transfer between servers.</p>
                    </div>
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#9881;</div>
                        <p class="bpos-feature__name">Full Order Lifecycle</p>
                        <p class="bpos-feature__desc">Modifiers, courses, split checks, comps, voids. Manager PIN authorization for sensitive actions.</p>
                    </div>
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#8857;</div>
                        <p class="bpos-feature__name">Offline-First</p>
                        <p class="bpos-feature__desc">Every feature works without internet. Data syncs automatically when connection returns.</p>
                    </div>
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#9783;</div>
                        <p class="bpos-feature__name">Kitchen Display (KDS)</p>
                        <p class="bpos-feature__desc">Station-based routing, course firing, timing alerts. Allergy banners that can&rsquo;t be missed.</p>
                    </div>
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#9733;</div>
                        <p class="bpos-feature__name">Bar Tabs & Happy Hour</p>
                        <p class="bpos-feature__desc">Named tabs, card holds, time-based pricing, age verification. Auto-close at last call.</p>
                    </div>
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#9636;</div>
                        <p class="bpos-feature__name">Reports & P&amp;L</p>
                        <p class="bpos-feature__desc">Daily sales, labor cost, COGS by category, margin tracking. ROI/PIP auto-calculated.</p>
                    </div>
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#9874;</div>
                        <p class="bpos-feature__name">Inventory & COGS</p>
                        <p class="bpos-feature__desc">Recipe-based auto-deduction, low stock alerts, waste logging. Track margins in real-time.</p>
                    </div>
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#9823;</div>
                        <p class="bpos-feature__name">Employee Management</p>
                        <p class="bpos-feature__desc">PIN login, time clock, tip pooling, payroll export with CPP/EI/WSIB. Performance tracking.</p>
                    </div>
                    <div class="bpos-feature">
                        <div class="bpos-feature__icon" aria-hidden="true">&#9842;</div>
                        <p class="bpos-feature__name">Runs on Any Device</p>
                        <p class="bpos-feature__desc">Desktop terminals, iPads, Android tablets, phones. Same app, same data, everywhere.</p>
                    </div>
                </div>
            </section>

            <div class="bpos-sweep" aria-hidden="true"></div>

            <!-- ── How It Works ──────────────────────────── -->
            <section class="bpos-how">
                <p class="bpos-how__label">Get Running in 3 Minutes</p>
                <div class="bpos-how__steps">
                    <div class="bpos-step">
                        <div class="bpos-step__num">1</div>
                        <p class="bpos-step__title">Sign Up</p>
                        <p class="bpos-step__desc">Start a 14-day free trial. No credit card required.</p>
                    </div>
                    <div class="bpos-step">
                        <div class="bpos-step__num">2</div>
                        <p class="bpos-step__title">Set Up</p>
                        <p class="bpos-step__desc">Add your menu, floor plan, and staff. The setup wizard walks you through it.</p>
                    </div>
                    <div class="bpos-step">
                        <div class="bpos-step__num">3</div>
                        <p class="bpos-step__title">Start Serving</p>
                        <p class="bpos-step__desc">Take orders, process payments, print receipts. Works immediately &mdash; even offline.</p>
                    </div>
                </div>
            </section>

            <div class="bpos-sweep" aria-hidden="true"></div>

            <!-- ── Pricing ───────────────────────────────── -->
            <section class="bpos-pricing" id="pricing">
                <p class="bpos-pricing__label">Pricing</p>
                <p class="bpos-pricing__headline">Simple pricing. No hidden fees.</p>

                <div class="bpos-toggle" id="bposToggle">
                    <button class="bpos-toggle__btn active" data-billing="month" type="button">Monthly</button>
                    <button class="bpos-toggle__btn" data-billing="year" type="button">
                        Annual
                        <span class="bpos-toggle__badge">Save 20%</span>
                    </button>
                </div>

                <p class="bpos-early-bird">
                    <span class="bpos-early-bird__tag">Limited</span>
                    Early Bird &mdash; First 10 customers lock in pricing for life
                </p>

                <div class="bpos-plans" id="bposPlans">
                    <!-- Standard -->
                    <div class="bpos-plan" data-plan="standard">
                        <p class="bpos-plan__name">Standard</p>
                        <p class="bpos-plan__tagline">Everything you need to run service</p>
                        <div class="bpos-plan__pricing">
                            <span class="bpos-plan__price-early" data-month="$59" data-year="$564">$59</span>
                            <span class="bpos-plan__price-regular" data-month="$99" data-year="$948">$99</span>
                            <span class="bpos-plan__period" data-month="/mo" data-year="/yr">/mo</span>
                        </div>
                        <p class="bpos-plan__savings" data-month="" data-year="Save $240/yr"></p>
                        <ul class="bpos-plan__features">
                            <li>Full POS &mdash; orders, payments, floor plan</li>
                            <li>Up to 3 terminals</li>
                            <li>Cash & card payments</li>
                            <li>Receipt printing</li>
                            <li>Basic reporting</li>
                            <li>Menu management</li>
                            <li>Employee PIN login</li>
                        </ul>
                        <a href="https://pos.bertrandgroup.ca" target="_blank" rel="noopener noreferrer" class="bpos-plan__cta bpos-plan__cta--primary">Try B-POS Free</a>
                    </div>

                    <!-- Pro -->
                    <div class="bpos-plan bpos-plan--featured" data-plan="pro">
                        <span class="bpos-plan__badge">Most Popular</span>
                        <p class="bpos-plan__name">Pro</p>
                        <p class="bpos-plan__tagline">Full suite for serious operations</p>
                        <div class="bpos-plan__pricing">
                            <span class="bpos-plan__price-early" data-month="$129" data-year="$1,236">$129</span>
                            <span class="bpos-plan__price-regular" data-month="$199" data-year="$1,908">$199</span>
                            <span class="bpos-plan__period" data-month="/mo" data-year="/yr">/mo</span>
                        </div>
                        <p class="bpos-plan__savings" data-month="" data-year="Save $480/yr"></p>
                        <ul class="bpos-plan__features">
                            <li>Everything in Standard, plus:</li>
                            <li>Unlimited terminals</li>
                            <li>Kitchen Display System (KDS)</li>
                            <li>Bar tabs & happy hour pricing</li>
                            <li>Inventory management</li>
                            <li>Advanced reporting & P&amp;L</li>
                            <li>Employee scheduling & tips</li>
                            <li>Gift cards & private room booking</li>
                            <li>Multi-terminal sync</li>
                            <li>Cloud backup</li>
                        </ul>
                        <a href="https://pos.bertrandgroup.ca" target="_blank" rel="noopener noreferrer" class="bpos-plan__cta bpos-plan__cta--primary">Try B-POS Free</a>
                    </div>
                </div>

                <p class="bpos-pricing__footnote">
                    14-day free trial. No credit card required. Cancel anytime.
                </p>
            </section>

            <div class="bpos-sweep" aria-hidden="true"></div>

            <!-- ── Custom Builds ───────────────────────────── -->
            <section class="bpos-custom">
                <div class="bpos-custom__card">
                    <p class="bpos-custom__title">Not a restaurant?</p>
                    <p class="bpos-custom__desc">B-POS is built for restaurant brands, but the engine is adaptable. We offer custom builds for salons, retail, and other service businesses &mdash; same offline-first reliability, tailored to your workflow.</p>
                    <a href="mailto:hello@bertrandgroup.ca?subject=Custom%20B-POS%20Build" class="bpos-custom__link">
                        Let&rsquo;s talk about your build
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                    </a>
                </div>
            </section>

            <div class="bpos-sweep" aria-hidden="true"></div>

            <!-- ── FAQ ───────────────────────────────────── -->
            <section class="bpos-faq">
                <p class="bpos-faq__label">Frequently Asked Questions</p>
                <div class="bpos-faq__list" id="bposFaq">
                    <div class="bpos-faq__item">
                        <button class="bpos-faq__q" type="button">
                            Does it really work offline?
                            <svg class="bpos-faq__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
                        </button>
                        <div class="bpos-faq__a">Yes. Every feature &mdash; orders, payments, printing, KDS &mdash; works without internet. Data syncs automatically when your connection returns. Your restaurant never stops taking orders.</div>
                    </div>
                    <div class="bpos-faq__item">
                        <button class="bpos-faq__q" type="button">
                            What hardware do I need?
                            <svg class="bpos-faq__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
                        </button>
                        <div class="bpos-faq__a">Any computer, tablet, or phone with a web browser. For receipts, a standard USB thermal printer. No proprietary hardware required &mdash; use what you already have.</div>
                    </div>
                    <div class="bpos-faq__item">
                        <button class="bpos-faq__q" type="button">
                            How long does setup take?
                            <svg class="bpos-faq__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
                        </button>
                        <div class="bpos-faq__a">The setup wizard takes about 3 minutes. Enter your restaurant name, build your floor plan, add menu items, and you&rsquo;re ready to serve. No IT person needed.</div>
                    </div>
                    <div class="bpos-faq__item">
                        <button class="bpos-faq__q" type="button">
                            Can I use it on multiple devices?
                            <svg class="bpos-faq__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
                        </button>
                        <div class="bpos-faq__a">Standard supports up to 3 terminals. Pro supports unlimited. All devices sync automatically over your local network &mdash; no cloud server required.</div>
                    </div>
                    <div class="bpos-faq__item">
                        <button class="bpos-faq__q" type="button">
                            What payment processors are supported?
                            <svg class="bpos-faq__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
                        </button>
                        <div class="bpos-faq__a">B-POS is payment-processor agnostic. We&rsquo;re integrating with Square, Stripe Terminal, and Clover first. Cash payments work from day one.</div>
                    </div>
                    <div class="bpos-faq__item">
                        <button class="bpos-faq__q" type="button">
                            Is my data safe?
                            <svg class="bpos-faq__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
                        </button>
                        <div class="bpos-faq__a">Your data lives on your own devices &mdash; not on our servers. Pro includes encrypted cloud backup for disaster recovery. Automatic local backups every 4 hours.</div>
                    </div>
                    <div class="bpos-faq__item">
                        <button class="bpos-faq__q" type="button">
                            Can I cancel anytime?
                            <svg class="bpos-faq__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
                        </button>
                        <div class="bpos-faq__a">Yes. No contracts, no cancellation fees. Your data remains on your devices even after cancellation.</div>
                    </div>
                </div>
            </section>
        </div>
    </main>

    <footer class="service-footer">
        <div class="service-footer__cta">
            <p class="service-footer__cta-text">Questions?</p>
            <a href="mailto:hello@bertrandgroup.ca" class="service-footer__cta-link">hello@bertrandgroup.ca</a>
        </div>
        <p class="service-footer__text">&copy; {currentYear} Bertrand Brands. All rights reserved.</p>
        <p class="footer-division"><a href="https://bertrandgroup.ca" target="_blank" rel="noopener noreferrer">A division of Bertrand Group Canada</a> &middot; <a href="/privacy">Privacy</a></p>
        <p class="service-footer__phone"><a href="tel:+17054133705" class="phone-link__mobile">(705) 413-3705</a><span class="phone-link__desktop">(705) 413-3705</span></p>
    </footer>

    <!-- Trial modal -->
    <div class="bpos-trial-overlay" id="bposTrialOverlay">
        <div class="bpos-trial-modal">
            <button class="bpos-trial-modal__close" type="button" id="bposTrialClose">&times;</button>
            <div id="bposTrialForm">
                <p class="bpos-trial-modal__title">Start your free trial</p>
                <p class="bpos-trial-modal__subtitle">14 days, no credit card. Your license key is generated instantly.</p>
                <input type="hidden" id="bposTrialPlan" value="standard" />
                <div class="bpos-trial-field">
                    <label for="bposTrialName">Restaurant Name</label>
                    <input type="text" id="bposTrialName" placeholder="e.g. Chez Marcel" required />
                </div>
                <div class="bpos-trial-field">
                    <label for="bposTrialContact">Your Name</label>
                    <input type="text" id="bposTrialContact" placeholder="First Last" required />
                </div>
                <div class="bpos-trial-field">
                    <label for="bposTrialEmail">Email</label>
                    <input type="email" id="bposTrialEmail" placeholder="you@restaurant.com" required />
                </div>
                <div class="bpos-trial-field">
                    <label for="bposTrialPhone">Phone <span style="font-weight:400;text-transform:none;letter-spacing:0">(optional)</span></label>
                    <input type="tel" id="bposTrialPhone" placeholder="(555) 123-4567" />
                </div>
                <button type="button" class="bpos-trial-submit" id="bposTrialSubmit">Get License Key</button>
                <p class="bpos-trial-error" id="bposTrialError"></p>
            </div>
            <div id="bposTrialSuccess" class="bpos-trial-success" style="display:none">
                <p class="bpos-trial-modal__title">You're in!</p>
                <p class="bpos-trial-modal__subtitle">Your 14-day trial license key:</p>
                <div class="bpos-trial-success__key" id="bposTrialKey"></div>
                <p style="font-size:13px;color:var(--text-muted,#999);margin-top:12px">
                    Enter this key when you launch B-POS.<br/>
                    We've also sent it to your email.
                </p>
                <a href="https://pos.bertrandgroup.ca" target="_blank" rel="noopener noreferrer" class="bpos-plan__cta bpos-plan__cta--primary" style="display:inline-block;margin-top:20px;padding:12px 32px;text-decoration:none">
                    Launch B-POS &rarr;
                </a>
            </div>
        </div>
    </div>

    <StickyMobileCTA
        href="https://pos.bertrandgroup.ca"
        text="Try B-POS Free"
        subtext="Live demo — no signup required"
        heroSelector=".bpos-hero"
        formSelector=".service-footer"
    />
</ServiceDetailLayout>

<script is:inline>
    (function() {
        var API_BASE = 'https://bpos-api.bertrandbrands.workers.dev';
        var currentBilling = 'month';

        /* Billing toggle */
        var toggle = document.getElementById('bposToggle');
        var grid = document.getElementById('bposPlans');
        if (toggle && grid) {
            var buttons = toggle.querySelectorAll('.bpos-toggle__btn');
            buttons.forEach(function(btn) {
                btn.addEventListener('click', function() {
                    currentBilling = btn.getAttribute('data-billing');
                    buttons.forEach(function(b) { b.classList.remove('active'); });
                    btn.classList.add('active');
                    grid.querySelectorAll('[data-' + currentBilling + ']').forEach(function(el) {
                        el.textContent = el.getAttribute('data-' + currentBilling);
                    });
                });
            });
        }

        /* FAQ accordion */
        var faq = document.getElementById('bposFaq');
        if (faq) {
            faq.querySelectorAll('.bpos-faq__q').forEach(function(btn) {
                btn.addEventListener('click', function() {
                    var item = btn.closest('.bpos-faq__item');
                    var wasOpen = item.classList.contains('is-open');
                    faq.querySelectorAll('.bpos-faq__item').forEach(function(i) { i.classList.remove('is-open'); });
                    if (!wasOpen) item.classList.add('is-open');
                });
            });
        }

        /* Trial modal */
        var overlay = document.getElementById('bposTrialOverlay');
        var closeBtn = document.getElementById('bposTrialClose');
        var formDiv = document.getElementById('bposTrialForm');
        var successDiv = document.getElementById('bposTrialSuccess');
        var submitBtn = document.getElementById('bposTrialSubmit');
        var errorEl = document.getElementById('bposTrialError');
        var planInput = document.getElementById('bposTrialPlan');

        function openTrialModal(plan) {
            planInput.value = plan;
            formDiv.style.display = '';
            successDiv.style.display = 'none';
            errorEl.style.display = 'none';
            overlay.classList.add('is-open');
            document.body.style.overflow = 'hidden';
        }

        function closeTrialModal() {
            overlay.classList.remove('is-open');
            document.body.style.overflow = '';
        }

        if (closeBtn) closeBtn.addEventListener('click', closeTrialModal);
        if (overlay) overlay.addEventListener('click', function(e) {
            if (e.target === overlay) closeTrialModal();
        });

        /* Trial form submit */
        if (submitBtn) {
            submitBtn.addEventListener('click', async function() {
                var name = document.getElementById('bposTrialName').value.trim();
                var contact = document.getElementById('bposTrialContact').value.trim();
                var email = document.getElementById('bposTrialEmail').value.trim();
                var phone = document.getElementById('bposTrialPhone').value.trim();

                if (!name || !contact || !email) {
                    errorEl.textContent = 'Please fill in all required fields.';
                    errorEl.style.display = 'block';
                    return;
                }

                submitBtn.disabled = true;
                submitBtn.textContent = 'Generating...';
                errorEl.style.display = 'none';

                try {
                    var res = await fetch(API_BASE + '/trial', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({
                            restaurantName: name,
                            contactName: contact,
                            email: email,
                            phone: phone || undefined
                        })
                    });

                    var data = await res.json();

                    if (!res.ok) {
                        throw new Error(data.error || 'Something went wrong');
                    }

                    document.getElementById('bposTrialKey').textContent = data.licenseKey;
                    formDiv.style.display = 'none';
                    successDiv.style.display = '';

                    /* GA4 event */
                    if (typeof gtag === 'function') {
                        gtag('event', 'generate_lead', {
                            event_category: 'bpos',
                            event_label: planInput.value + '_trial',
                            value: 0
                        });
                    }
                } catch (err) {
                    errorEl.textContent = err.message || 'Failed to generate trial. Please try again.';
                    errorEl.style.display = 'block';
                } finally {
                    submitBtn.disabled = false;
                    submitBtn.textContent = 'Get License Key';
                }
            });
        }

        /* Subscribe buttons → Stripe Checkout */
        document.querySelectorAll('.bpos-plan__cta--subscribe').forEach(function(btn) {
            btn.addEventListener('click', async function() {
                var plan = btn.getAttribute('data-plan');
                btn.disabled = true;
                btn.textContent = 'Redirecting...';

                try {
                    var res = await fetch(API_BASE + '/checkout', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({
                            plan: plan,
                            billing: currentBilling,
                            earlyBird: currentBilling === 'year',
                            successUrl: window.location.origin + '/bpos?checkout=success',
                            cancelUrl: window.location.origin + '/bpos?checkout=cancelled'
                        })
                    });

                    var data = await res.json();
                    if (!res.ok) throw new Error(data.error);

                    window.location.href = data.url;
                } catch (err) {
                    alert('Could not start checkout: ' + (err.message || 'Please try again.'));
                    btn.disabled = false;
                    btn.textContent = 'Subscribe';
                }
            });
        });

        /* Trial buttons → open modal */
        document.querySelectorAll('.bpos-trial-btn').forEach(function(btn) {
            btn.addEventListener('click', function() {
                openTrialModal(btn.getAttribute('data-plan'));
            });
        });

        /* Handle checkout success/cancel URL params */
        var params = new URLSearchParams(window.location.search);
        if (params.get('checkout') === 'success') {
            alert('Payment successful! Check your email for your license key.');
            window.history.replaceState({}, '', '/bpos');
        }
    })();
</script>
